{
    "build": {
      "env": {
        "NODE_ENV": "production"
      },
      "installCommand": "npm install --legacy-peer-deps",
      "buildCommand": "vercel build"
    },
    "functions": {
        "src/api/**/*.ts": {
          "maxDuration": 500
        }
      }
    
  }
  